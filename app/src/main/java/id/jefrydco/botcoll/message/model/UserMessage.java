package id.jefrydco.botcoll.message.model;

public class UserMessage extends BaseMessage {

    public UserMessage(String id, String message, long createdAt) {
        super(id, message, createdAt);
    }
}
